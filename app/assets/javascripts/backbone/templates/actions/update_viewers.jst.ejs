<%= action.get('user').escape('name') %>
<% if (action.escape('added') && action.escape('added').length > 0) { %>
  added
  <%= action.escape('added').map(function(p) { return p.escape('name'); }).join(', ') %>
  as a viewer
  <% if (action.escape('removed') && action.escape('removed').length > 0) { %>
    and
  <% } %>
<% } %>
<% if (action.escape('removed') && action.escape('removed').length > 0) { %>
  removed
  <%= action.escape('removed').map(function(p) { return p.escape('name'); }).join(', ') %>
  as a viewer
<% } %>
