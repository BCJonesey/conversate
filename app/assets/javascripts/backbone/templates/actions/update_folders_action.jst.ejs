<% if (action.escape('addedViewers') && action.escape('addedViewers').length > 0) { %>
  <%= action.escape('addedViewers').map(function(t) { return t.escape('name') }).join(', ') %>
  can now see this conversation.
<% } %>
