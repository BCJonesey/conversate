<% if (action.get('addedViewers') && action.get('addedViewers').length > 0) { %>
  <%= action.get('addedViewers').map(function(t) { return t.escape('name') }).join(', ') %>
  can now see this conversation.
<% } %>
